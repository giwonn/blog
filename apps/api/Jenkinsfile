pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Ensure Network') {
            steps {
                sh 'docker network create blog-network || true'
            }
        }

        stage('Blue-Green Deploy') {
            steps {
                sh 'chmod +x infra/scripts/deploy-api.sh && bash infra/scripts/deploy-api.sh'
            }
        }
    }

    post {
        success {
            echo 'API deployment successful!'
        }
        failure {
            echo 'API deployment failed!'
        }
    }
}
